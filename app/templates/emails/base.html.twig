<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #f5f5f5; padding: 15px; text-align: center; }
        .content { padding: 20px; }
        .footer { text-align: center; padding: 10px; font-size: 12px; color: #777; }
        .footer-links { margin-top: 10px; }
        .subscription-card { background: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>{% block header %}{% endblock %}</h2>
        </div>

        <div class="content">
            {% block content %}{% endblock %}
        </div>

        <div class="footer">
            <p>© {{ "now"|date("Y") }} Все права защищены.</p>
            <div class="footer-links">
                <a href="{{ url('subscriptions') }}">Управление подписками</a> | 
                <a href="{{ url('unsubscribe_all', {email: user_email|default('')}) }}">Отписаться от всех</a>
            </div>
        </div>
    </div>
</body>
</html>